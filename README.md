# Configuring local tools

Reference: https://jekyllrb.com/docs/installation/ubuntu/

# Testing the site locally

```shell
./run-test.sh
```

# Publishing

```shell
git add .
git commit -m 'add content to blog'
git push -u origin gh-pages
```